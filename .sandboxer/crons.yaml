# Example cron configuration for Sandboxer

crons:
  # Example: Condition that FAILS - job is skipped
  # - name: monitor-api
  #   schedule: "*/15 * * * *"
  #   type: claude
  #   prompt: "API is down, investigate"
  #   condition: "! curl -sf http://localhost:3000/health"

  # Example: Condition that PASSES - job runs
  # - name: daily-review
  #   schedule: "0 9 * * *"
  #   type: claude
  #   prompt: "Review code changes"
  #   condition: "git status --porcelain | grep -q ."

  # Example: No condition - always runs on schedule
  # - name: backup
  #   schedule: "0 3 * * *"
  #   type: bash
  #   command: "./scripts/backup.sh"
